# QA-102 source inputs for the isolated Python 3.12 transformer lock.
-r ../requirements-transformer-tokenizer.txt

# PyTorch is installed separately with --no-deps from the isolated CUDA lock.
# Declare its required Python-side dependencies here so they resolve only from
# PyPI and are protected by the transformer lock's hashes.
filelock
fsspec>=2026.4.0
jinja2
networkx>=2.5.1
setuptools>=77.0.3
sympy>=1.13.3
typing-extensions>=4.10.0
