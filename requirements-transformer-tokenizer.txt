# CT-301 source constraints retained for dependency-policy provenance. For
# installations, use requirements/locks/transformer-py312-win-amd64.lock.txt
# as documented in docs/reproducible_environments.md.
# PyTorch remains isolated in its own hash-enforced lock.
transformers==5.14.1
tokenizers==0.22.2
huggingface-hub==1.24.0
safetensors==0.8.0
